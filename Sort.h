#ifndef SORT_H
#define SORT_H

#include <vector>

class Sort
{
private:
    std::vector<int> data;
public:
    Sort(/* args */);
    ~Sort();
    std::vector<int> sort(std::vector<int> list);
};

Sort::Sort(/* args */)
{
}

Sort::~Sort()
{
}
std::vector<int> Sort::sort(std::vector<int> list)
{
    
}

#endif